# ═══════════════════════════════════════════════════════════════════════════════
# requirements-minimal.txt — install this FIRST if full install fails
# Then install the AI packages separately (requirements-ai.txt)
# ═══════════════════════════════════════════════════════════════════════════════
dotenv
flask
flask-jwt-extended
flask-cors
flask-Limiter
flask-swagger-ui
Werkzeug==3.1.3
waitress==3.0.2
oracledb==2.5.1
psycopg2-binary
bcrypt
python-dotenv==1.1.0
pandas
requests==2.32.3
pydantic
