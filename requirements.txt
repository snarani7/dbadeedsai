# ═══════════════════════════════════════════════════════════════════════════════
# dbadeeds.ai — requirements.txt  (Windows + Linux compatible, Python 3.10-3.14)
# 
# INSTALL STEPS:
#   pip install -r requirements.txt
#   pip install -r requirements-google.txt   (optional: only if using Gemini/Vertex)
#
# If pip fails with "resolution-too-deep", use uv instead:
#   pip install uv
#   uv pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════════════════

# ── Core Flask ────────────────────────────────────────────────────────────────
Flask==3.1.0
flask-jwt-extended==4.7.1
flask-cors==5.0.1
Flask-Limiter==3.9.4
flask-swagger-ui==4.11.1

# ── WSGI servers ──────────────────────────────────────────────────────────────
waitress==3.0.2
gunicorn==23.0.0

# ── Security ──────────────────────────────────────────────────────────────────
bcrypt==4.3.0
python-dotenv==1.1.0

# ── Database drivers ──────────────────────────────────────────────────────────
psycopg2-binary
oracledb==2.5.1

# ── LangChain core ────────────────────────────────────────────────────────────
langchain==0.3.25
langchain-core==0.3.56
langchain-community==0.3.24

# ── LLM providers (install what you need) ────────────────────────────────────
langchain-openai==0.3.16
langchain-anthropic==0.3.15
langchain-groq==0.3.2
langchain-ollama==0.3.2

# ── LLM SDKs ─────────────────────────────────────────────────────────────────
openai==1.78.1
anthropic==0.52.0
groq==0.25.0

# ── Data & utilities ─────────────────────────────────────────────────────────
pandas==2.2.3
requests==2.32.3
pydantic==2.11.4
python-dateutil==2.9.0.post0
APScheduler==3.10.4

# NOTE: Google Gemini + Vertex AI — install separately to avoid conflicts:
#   pip install -r requirements-google.txt
